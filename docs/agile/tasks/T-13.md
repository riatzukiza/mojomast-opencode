---
id: T-13
title: Chat message send macro bench (HTTP)
status: TODO
blocked_by: [T-02, T-04]
blocks: [T-16, T-18]
tags: [perf, macro, http, chat]
---

## Description
Benchmark `POST /session/:id/message` end-to-end latency and throughput using autocannon with realistic payloads.

## Requirements
- [ ] Scenario JSON `perf/tools/autocannon/chat-loop.json` (duration, connections, payload template).
- [ ] `perf/macro/http-message-send.bench.ts` boots the opencode server and runs the scenario.
- [ ] Capture metrics: p50/p95/p99 latency, RPS, error rate, queue wait (when multiple requests target same session).
- [ ] Emit JSON to `perf/regression/macro-http-message.json` including Node/Bun versions and NODE_ENV.

## DoD
- [ ] Reproducible numbers across runs; params configurable via env.

## Related
- packages/opencode/src/server/server.ts:831-904 (endpoint)
