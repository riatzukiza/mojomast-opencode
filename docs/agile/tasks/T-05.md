---
id: T-05
title: Memory & leak test harness (perf/memory)
status: TODO
blocked_by: [T-02, T-01]
blocks: [T-06, T-10, T-11, T-12]
tags: [perf, memory, leaks]
---

## Description

Create a memory/leak testing harness that runs workloads, samples heap usage, and flags suspicious growth.

## Requirements

- [ ] `perf/memory/harness.ts` to run workload, sample heap, optional `global.gc()`.
- [ ] Implement `login-leak.spec.ts` or chat-loop leak spec.
- [ ] Heuristic: ignore warmup; assert growth ratio < threshold.
- [ ] Optional: heap snapshot via `heapdump` on failure.

## DoD

- [ ] `pnpm perf:memory` runs a scenario and passes on healthy build; fails on artificial leak.
