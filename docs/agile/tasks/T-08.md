---
id: T-08
title: Regression harness & CI wiring (perf/regression)
status: TODO
blocked_by: [T-03, T-04, T-05, T-07]
blocks: [T-10, T-11, T-12]
tags: [perf, regression, ci]
---

## Description
Implement a central regression checker that consumes JSON outputs and fails CI on large regressions.

## Requirements
- [ ] Define `PerfResult` schema; implement `check-regressions.ts` (compare with budgets, `--update-baseline`).
- [ ] Add `pnpm perf:check` and a CI job to run a reduced suite.

## DoD
- [ ] Local threshold breaks fail `perf:check`; CI enforces subset baselines.
