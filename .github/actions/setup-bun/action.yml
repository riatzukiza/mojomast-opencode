name: setup-bun
description: Setup Bun and install workspace dependencies (back-compat)
inputs:
  bun-version:
    description: Version of Bun to install (e.g., 1.3.1 or latest)
    required: false
    default: 'latest'
runs:
  using: composite
  steps:
    - name: Setup Bun
      uses: oven-sh/setup-bun@v2
      with:
        bun-version: ${{ inputs.bun-version }}

    - name: Cache Bun install dir
      uses: actions/cache@v4
      with:
        path: ~/.bun
        key: ${{ runner.os }}-bun-${{ inputs.bun-version }}
        restore-keys: |
          ${{ runner.os }}-bun-

    # Backwards compatibility: this action historically ran `bun install`.
    # Many existing workflows rely on that behavior.
    - name: Install workspace dependencies
      shell: bash
      run: bun install --frozen-lockfile || bun install
